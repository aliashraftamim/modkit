export const validateRequest = (fileName: any): any => {
  return fileName;
};

export const auth = (fileName: any): any => {
  return fileName;
};
export const sendResponse = (n1: any, n2: any): any => {
  return { n1, n2 };
};

export const catchAsync = (n1: any): any => {
  return { n1 };
};
